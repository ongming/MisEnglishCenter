# ===========================
# DataSource (MySQL)
# ===========================
spring.datasource.url=jdbc:mysql://localhost:3306/mis_language_center?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
# JPA / Hibernate
# ===========================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Không c?n khai báo dialect ? Hibernate t? phát hi?n t? MySQL connector

# ===========================
# T?t web server (d?ng cho JavaFX desktop app)
# ===========================
spring.main.web-application-type=none

